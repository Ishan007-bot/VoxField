# Copy to .env.local for local overrides, or set in the Vercel dashboard.
# Point the frontend at your deployed backend (Render) URL.
# Leave unset for local dev — the Vite proxy forwards /api to localhost:8000.
VITE_API_BASE=https://voxfield-api.onrender.com
